#ifndef ENUM_H
#define ENUM_H

enum class Button {
	ManualOverride, Poke, ShutUp, Summon, Joystick, COUNT
};

enum class LEDs {
	BigGreen, Wifi, Power, BatteryLow, TftBacklight, COUNT
};

enum class RGB_LEDs {
};

#endif //ENUM_H
